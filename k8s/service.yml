apiVersion: v1
kind: Service
metadata:
  name: dasc
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dasc"
    port: 8080
  selector:
    app: dasc
